# Screenshots

The main README references three images in this folder. Save the screenshots
here with these exact names:

| Filename        | Shot                                                                 |
| --------------- | -------------------------------------------------------------------- |
| `bust.png`      | The marble bust in profile against the sunset (the "roses" frame).   |
| `colonnade.png` | The avenue of columns receding toward the sun (the "apocalypse" frame). |
| `player.png`    | A gameplay frame showing the now-playing bar + year scrubber (the "redbone" frame). |

PNG or JPG both work — if you use `.jpg`, update the extensions in `README.md`.
